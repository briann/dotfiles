# Use 1Password to generate this.
# export SSH_AUTH_SOCK=...

alias ls="eza"
alias ll="eza -l --icons=auto --group-directories-first"

# Mise: used for node, python, poetry, golang
# (Rust is via rustup)
eval "$(/Users/brian/.local/bin/mise activate zsh)"
